# Example research skill

Use `search` only for read-only research. Cite the returned URLs, distinguish retrieved facts from
inference, and ask before sending sensitive query text to the remote connector.
